package transforming_observables;

/**
 * Description：flatMap ，将一个observable转变成多个observable
 * Create Time：2017/5/24 13:08
 * Author:KingJA
 * Email:kingjavip@gmail.com
 */
public class flatMap {
    public static void main(String[] args) {
//        Flowable.fromArray(DataManager.getPerson(5)).flatMap(new Function<Person, Publisher<Course>>() {
//            @Override
//            public Publisher<Course> apply(Person person) throws Exception {
//                return Flowable.fromArray(person.getCourses());
//            }
//        }).subscribe()
    }
}
